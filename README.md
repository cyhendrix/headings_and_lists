# Headings and Lists Practice Lab

Interactive practice for HTML heading hierarchy (`h1`–`h6`), ordered and unordered lists (`ol`, `ul`), list items (`li`), body placement, debugging, and code creation.

The GitHub Pages source is the root `index.html` file.
